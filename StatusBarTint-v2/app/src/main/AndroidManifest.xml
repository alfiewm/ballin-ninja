<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="meng.statusbartint">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.RedActivity" />
        <activity android:name=".ui.WhiteActivity" />
        <activity
            android:name=".ui.SplashActivity"
            android:theme="@style/SplashTheme" />
        <activity
            android:name=".ui.SolidStatusBarActivity"
            android:theme="@style/SolidAppTheme" />
        <activity
            android:name=".ui.OmniThemeActivity"
            android:theme="@style/OmniTheme" />
        <activity
            android:name=".ui.OmniThemeWithHeadActivity"
            android:theme="@style/OmniTheme" />
    </application>

</manifest>